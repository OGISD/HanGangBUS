name: 프로젝트 검사

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: 저장소 받기
        uses: actions/checkout@v6
      - name: 코드와 조석표 검사
        shell: pwsh
        run: ./tools/check-project.ps1
