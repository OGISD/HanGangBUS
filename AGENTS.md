# AGENTS.md — 한강버스 운항 도우미

이 파일은 Codex가 프로젝트의 핵심 규칙과 현재 상태를 빠르게 확인하기 위한 문서입니다.
모든 설명과 문서는 한국어로 작성합니다.

## 작업 규칙

- 작업을 완료할 때마다 `AGENTS.md`, `AI_HANDOFF.md`, `TASKS.md`, `CHANGELOG.md`, `CLAUDE.md`를 확인하고
  바뀐 기능·확정값·배포 상태·다음 할 일을 같은 변경에 반영합니다.
- 사용자는 코딩 초보이므로 쉬운 말로 설명하고, 변경한 파일과 이유를 함께 알려줍니다.
- 안전 관련 수치와 판정 기준을 근거 없이 변경하지 않습니다.
- 웹앱 로직은 `index.html` 하나에 유지합니다. 연간 조석표처럼 큰 정적 데이터만 `data/*.json`으로 분리합니다.
- 완료 전 로컬 미리보기와 데이터 무결성 검사를 수행합니다.

## 프로젝트 요약

한강버스 운항에 참고할 정보를 한 화면에 표시하는 모바일 웹앱입니다.

- 잠수교·행주대교 통과높이와 운항 판정
- 팔당댐 방류량 현재/4시간 전
- 인천·여의도 물때와 조류세기
- 행주대교·한강대교 수위 및 역류/순류 경향
- GitHub Pages에 정적 배포

## 배포 상태

- 공개 주소: https://ogisd.github.io/HanGangBUS/
- 저장소: https://github.com/OGISD/HanGangBUS
- 브랜치: `main`
- 배포: `git push` 후 GitHub Pages 자동 반영
- 현재 확정 기준점: `v0.6.0`(2026-07-27)

## 핵심 통과높이 공식

```text
통과높이 = 기준높이(base) - 현재 수위(wl) + 오차보정(offset)
기준높이 = 교량 형하 표고 - 수위계 영점표고(gdt)
```

| 항목 | 잠수교 | 행주대교 |
|---|---:|---:|
| 관측소 | 1018680 | 1019630 |
| base | 11.76 | 13.40 |
| offset | +0.02 | 0 |
| 운항금지선 | 7.3 | 10.9 |
| 경고선 | 7.8 | 10.9(경고 단계 없음) |
| 저수위경고선 | 9.1 | 없음 |

- 교량 설정 저장 키: `hb_bridges7`
- 팔당댐 코드: `1017310`, 운항금지 방류량: `3000㎥/s`, 주의선: `null`
- 신선도 경고: 30분
- 역류 추세: 최근 40분 최소제곱 기울기, 정체 임계 `±4cm/h`
- 시간차 상관 최소값: `0.5`

## API와 네트워크 결정

- HRFCO 수위·댐 API는 해외 IP를 차단하므로 방문자 브라우저가 직접 호출합니다.
- Cloudflare Worker·Vercel 등 해외 프록시는 HRFCO에 사용하지 않습니다.
- HRFCO 키는 공개·읽기 전용 성격을 감수하고 `index.html`에 포함되어 있습니다.
- 물때는 공공데이터포털 국립해양조사원 고·저조 API를 사용합니다.
- 조류세기는 KHOA 바다누리 수치조류도 API를 사용합니다.

## 물때 API 장애 백업(2026-08-03)

- 공공 API를 항상 먼저 사용합니다.
- API가 CORS·통신·형식 오류로 실패하면 같은 GitHub Pages의 연간 조석표 JSON으로 자동 전환합니다.
- 정적 백업:
  - `data/tide-incheon-2026.json`: 2026년 8~12월, 153일
  - `data/tide-incheon-2027.json`: 2027년 1~12월, 365일
- 카드 우측 기존 날짜 영역에 `MM/DD · 온라인` 또는 `MM/DD · 연간표`로 출처를 표시합니다.
- 둘 다 조석 예보이므로 `실시간`이라는 표현을 사용하지 않습니다.
- 백업 자료도 기존 `renderTide()`를 거쳐 여의도 +4시간과 역류 위상 계산에 동일하게 사용합니다.
- 백업은 API 캐시에 저장하지 않아 10분 새로고침 때 API를 다시 시도하고, 복구되면 자동으로 돌아갑니다.
- 오늘 첫 물때의 증감은 브라우저에 저장된 어제 마지막 조위를 우선 사용하고, 없으면 연간표의 어제 마지막 조위로 보충합니다.
  따라서 전날 앱을 열지 않았거나 온라인·연간표가 전환되어도 첫 행 증감이 유지됩니다. 연간표 시작 전날 자료가 없는
  `2026-08-01`만 보충할 수 없습니다.
- 원본 월별 텍스트는 저장소에 올리지 않습니다. 재생성은 `tools/convert-tide.ps1`을 사용합니다.

## 주요 파일

- `index.html`: 웹앱 로직과 UI
- `data/tide-incheon-2026.json`, `data/tide-incheon-2027.json`: 연간 조석표 백업
- `tools/convert-tide.ps1`: 월별 조석표 ZIP 변환·검증 도구
- `AI_HANDOFF.md`: 새 세션에서 가장 먼저 읽을 인수인계
- `CLAUDE.md`: 상세 기술 사양과 결정 기록
- `TASKS.md`: 진행·완료·보류 작업
- `CHANGELOG.md`: 변경 이력
- `RULES.md`: 프로젝트 작업 규칙
- `PRD.md`: 제품 요구사항

## 현재 다음 할 일

- 실제 역류 사례로 `FLOW.winMin`, `deadRate`, `minCorr` 보정
- 새 교량 추가 시 코드·확정값 표·Q&A·문서를 함께 갱신
- 2028년 조석표가 발간되면 같은 변환 도구로 JSON 추가
- 물때 API의 브라우저 CORS 차단이 일시 장애인지 지속 정책인지 관찰
