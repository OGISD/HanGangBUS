# AI_HANDOFF.md

> 이 문서는 세션(대화) 간, 그리고 서로 다른 AI 도구(Claude Code, Codex, ChatGPT 등) 간 인수인계를 위한 문서입니다. 새 세션을 시작할 때는 항상 이 문서부터 읽게 하세요. 세션을 마칠 때는 항상 이 문서를 갱신하세요.

---

# 프로젝트명

한강 교량 통과높이 모니터 (HanGangBUS)

---

# 프로젝트 목표

잠수교·행주대교의 선박 통과높이(= 기준높이 − 실시간 수위)를 계산해 안전 판정과 함께
보여주는 단일 파일 모바일 웹앱. 소수 내부 공유용, 이미 라이브 배포 중.
→ 상세: [PRD.md](PRD.md)

---

# 현재 진행 상황

- **앱은 완성되어 라이브 상태**: https://ogisd.github.io/HanGangBUS/ (GitHub Pages)
- 자동 모드(HRFCO API 직접 호출) 실시간 작동 검증 완료, 두 교량 기준값 모두 verified
- **카드 확장 완료(2026-07)**: 팔당댐 방류 · 잠수교 · 여의도/인천 물때 · 조류세기 · 한강 수위(2열) · 역류/순류 판정 v1.
  최신 사양·확정값은 항상 CLAUDE.md 기준. (초기 2개 교량 통과높이 → 종합 운항 도우미로 확장됨)
- 2026-06-22 배포 자동화(git push)·안전 기능 2종 / 2026-07-04 AI Development OS 문서 체계 적용

---

# 최근 완료한 작업

- **물때 API 장애 백업 (2026-08-03)**: data.go.kr 물때 API가 브라우저 Origin 요청을 403/CORS로 차단한 사례에 대응.
  API를 계속 우선 사용하되 실패하면 같은 GitHub Pages의 연간 조석표 JSON으로 자동 전환한다.
  `data/tide-incheon-2026.json`은 8~12월 153일, `data/tide-incheon-2027.json`은 전체 365일이며 원본 텍스트는 미포함.
  카드 기존 날짜 자리에 `MM/DD · 온라인`/`MM/DD · 연간표`로 출처 표시(세로 공간 추가 없음). 백업값도 `renderTide`와
  `tidePhaseNow`를 그대로 거쳐 여의도 +4시간·역류 위상에 반영. `tools/convert-tide.ps1`로 월별 ZIP을 재생성 가능.

- **Q&A 점검·보강 (2026-07-27)**: 전 항목을 코드값과 대조 → **오류 없음**(7.3/7.8/9.1·10.9·base·3000·30분 등 일치).
  누락된 최신 기능 3개 추가(17→20): 역류/순류 배지 뜻, 추세 화살표(cm/h)와 '선행 N분(조석/방류 영향 추정)',
  홈 화면 바로가기. 카드 목록에도 '역류/순류 판정 표시' 명기. 문구는 `renderFlow()` 실제 출력과 글자 그대로 대조·검증함.
  ※ **관리자 진단줄(#flowDiag)·이스터에그는 의도적으로 Q&A에 넣지 않음**(숨은 기능이므로 유지할 것).

- **문구 개선 + 관리자 진단줄 (2026-07-27)**: 역류 근거줄의 "조석 서명/상류 서명"을 **"조석 영향 추정/방류 영향 추정"**
  으로 교체(‘서명’은 signature 직역이라 이질적 — 사용자 지적). 원인 해석엔 **항상 ‘추정’을 붙일 것**.
  + `#flowDiag` 관리자 진단줄 추가(`setFlowDiag()`, `applyAdmin()`이 `#setPanel`과 함께 토글) — cm/h·임계값·선행 분·
  상관·진폭·위상 원시 수치 노출. `?admin`에서만 보임. 실제 역류 목격 시 캡처 → 임계값 보정 근거.

- **역류 판정 v2 (2026-07-27)**: ① **시간차 교차검증**(`leadLag`) — 두 다리 움직임을 교차상관해 행주 선행
  (→ "조석 영향 추정") vs 한강대교 선행(→ "방류 영향 추정") 구분. 역류 판정인데 한강대교 선행이면
  '상류(방류) 영향 가능' 덧붙임. ② **추세 견고화**(`wlTrend`) — 2점 차분 → 최근 40분 최소제곱 기울기(cm/h). 새 API 0.
  부호 규칙 합성 검증 완료(+30분/−30분, corr 1.0). 실측 행주 선행 40분·corr 0.85.
  ※ 검토했으나 **채택 안 한 것**: 신곡보 720cm 게이팅·조위 보간(보 게이팅을 정확히 알 수 없음), 팔당댐 방류 보정
  (영향 크기 정량화 근거 없음), 지속성 필터(실용성 의문) — 모두 사용자 판단.

- **API 최적화: 물때 하루 1번 캐시 (2026-07-15)**: `fetchTide`가 매 새로고침(10분)마다 같은 하루치 고저조를
  재요청하던 낭비 제거 → `hb_tide`(ymd 키) 캐시(조류세기 `hb_tidecur`와 동일 패턴). 캐시 히트 시 네트워크 0,
  `renderTide`만 재실행해 화면·역류 위상 갱신. 탭 종일 ~144→1회/일. HRFCO는 새로고침당 4회로 이미 최소(재확인).
- **이스터에그 개선 + 버그수정 (2026-07-15)**: 크레딧 마퀴 글자 반응형(`clamp(11px,3.5vw,16px)`), 앞머리 배 ⛴
  (bob 애니)+물빛 배경, 트리거 30초→**1분**. 이후 `.marquee span`(padding-left:100%)이 배 span에도 걸려 이름이
  사라지던 버그를 `.marquee>span`(직계 자식)으로 수정.
- **행주대교 운항금지선 11.2→10.9 m (2026-07-15)**: `DEFAULT_BRIDGES` noGo/warn 10.9, Q&A 갱신, 저장키
  hb_bridges6→**hb_bridges7** bump(예전 값 무시). 상태줄 "운항금지선(10.90m)까지"는 자동 반영.
- **한강 역류/순류 판정 v1 (2026-07-15)**: 한강 수위 카드에 종합 배지(`#flowBadge`)+다리별 추세 화살표.
  판정 = 수위 추세(`wlTrend`, 최근 30분 Δcm) + 인천 조위 위상(`tidePhaseNow`, 밀/썰물) 교차검증 → `renderFlow`.
  밀물&행주↑=역류 경향, 썰물&행주↓=순류, 상충=정체·전환점(경향형). 새 API 없음(기존 rows·물때 재활용).
  전역 `flow`, 상수 `FLOW`(winMin30·deadCm2). 실데이터 검증 완료(밀물↔행주하강 위상차를 정체로 올바르게 판정).
  한계: 한강대교 조석반응 실측 미확인·720cm 게이팅 후속 → 임계값 보정 예정. 문구는 경향형 유지.

- **Q&A 허위 기재 수정 (2026-07-15)**: 인천 카드 설명의 "일출·일몰·월출·월몰"은 **실제로 없는 기능**이라 삭제.
  (조류세기가 대체하며 제거됨 — `#sunMoon` HTML 없음, `fetchRiseSet()`은 미호출 죽은 코드.) 원인이던
  **CLAUDE.md의 모순 기록도 '❌ 없는 기능'으로 정정**. ⚠ 앞으로 인천 카드에 출몰시각이 있다고 쓰지 말 것.
  더불어 자동/수동 Q에 '한강 수위도 자동 전용' 명시, 팔당댐 Q에 '과거 4시간 기록' 언급 추가.
- **수위 카드 문구 정리 + Q&A 개정 (2026-07-13)**: 카드 `#wlNote`에서 '마포대교 대체' 설명 제거→Q&A로 이전
  (새 항목 '한강 수위 카드는 뭔가요?'). 조류세기 Q&A를 실제(KHOA 예측 유속 오늘 최고값)로 정정(옛 조차 추정 오류).
  카드 목록·인트로에 '한강 수위' 추가. 미리보기 검증 완료.
- **바로가기 버튼 ✕ 스누즈화 (2026-07-13)**: 영구 숨김 → 7일 스누즈(`hb_a2hs`=만료 timestamp). 실수로 눌러도
  일주일 뒤 자동 재표시. 옛 `'0'`값은 자동 복원. 미리보기로 표시/숨김/만료복구/마이그레이션 검증 완료.
- **행주 HRFCO 중복 호출 제거 (2026-07-13)**: 행주(1019630)를 통과높이용·수위카드용으로 두 번 부르던 것을
  4시간 조회 1건으로 통합(최신값으로 통과높이 카드도 렌더). 새로고침당 HRFCO 5→4회. `fetchWL`에 `bridge` 인자
  추가, `fetchAll`이 행주 `fetchBridge` 건너뜀. 미리보기 network(performance)로 4건 확인 완료.
- **한강 수위 카드 2열 합침 (2026-07-13)**: 행주대교·한강대교 수위 카드를 한 카드(`#wlCard`)로 합쳐
  팔당댐 방류 카드처럼 좌우 2열(행주 | 한강대교)로 표시. 공간 절약. 신선도는 공용 `#wlT`. 미리보기 검증 완료.
- **홈 화면 바로가기 버튼 (2026-07-13)**: 면책 문구 아래 슬림 바(`#a2hs`). 안드로이드/크롬은 네이티브
  설치창(`beforeinstallprompt`), 아이폰(사파리)은 "공유→홈 화면에 추가" 안내 토글, 그 외 브라우저는 메뉴 안내.
  이미 설치(standalone)면 숨김, ✕로 닫으면 `hb_a2hs='0'` 영구 숨김. 미리보기 검증 완료.
- **한강대교 수위 카드 + UI 다듬기 (2026-07-13)**: 행주대교 수위 카드 밑에 **한강대교 수위 카드**(`#hangangCard`)
  추가 — 마포대교는 HRFCO 관측소가 없어(서울 본류 = 잠수교/한강대교/행주대교뿐) 가장 가까운 한강대교(1018683,
  마포 상류 ≈3km, 수위 사실상 동일)로 대체, 라벨·안내에 명시. 최근 30분 요약 + 과거 4시간 펼치기.
  행주/한강대교 로직을 공용 `wlUrl()/fetchWL()/renderWL()`로 일반화(기존 backflow 함수 대체). 미리보기 검증 완료.
  + 접이식 화살표(▼/▲) 확대, 새로고침 버튼 진행/완료 피드백(⟳ 새로고침 중… → ✓ 새로고침됨).
- **조류세기 실측 전환 (2026-07-07)**: 인천 카드 하단을 KHOA 수치조류도 예측 유향·유속으로 교체.
  인천 지점 실시간 유속(cm/s)+방향 화살표+막대. KHOA 바다누리 자체 키(별개, ~2027-07-06 만료).
  일출/일몰(출몰) 표시는 이걸로 대체됨(코드는 보존). 실 API 검증 완료.
- **직원 피드백 2차·UI 다듬기 (2026-07-06)**: 헤더 한 줄(제목+제작자+토글), 조류 게이지 정렬, 다리 그림
  120px 축소(숨겨도 통과높이 중앙 표시), 하단 문구 삭제(자리 보존), 팔당댐 '운항금지'(여의도 제거),
  도움말 제목 '· Q&A' + Q&A 전면 개정(카드 기능·색상 상태 의미·용어·형하−gdt 계산식).
- **직원 피드백 1차 반영 (2026-07-06)**: 앱 이름 "한강버스 운항 도우미"+제작자 표기, 잠수교 금지 7.3/경고 7.8/
  오차보정 +2cm(설정 조정), 행주 금지 11.2·경고 제거, 상태줄 "운항금지선(N m)까지" 표기, 여의도 만조 2개로
  복귀, 다리 그림 축소+숨기기 토글, Q&A 갱신, 교량 저장키 hb_bridges5. 미리보기 검증 완료.
- **일출·일몰·월출·월몰 (2026-07-06)**: 한국천문연구원 출몰시각 API(data.go.kr B090041, location=인천)로
  인천 물때 카드 하단에 통합(별도 카드 아님, 사용자 선택 옵션 A). 물때와 같은 계정 키. 실 API 검증 완료.
- **인천·여의도 물때 카드 (v0.5.0, 2026-07-06)**: KHOA 조석예보(고저조) API(data.go.kr 1192136,
  인천 DT_0001)로 인천/여의도 각 하루 물때 4개(만조·간조) 표시. 여의도=인천+4시간. 각 행에 화살표
  (▲만조/▼간조)+직전 대비 증감(cm). 첫 행 증감은 어제 마지막 물때를 localStorage 캐시로 계산.
  XML→DOMParser, CORS 직접 호출. 화이트보드 순서로 배치. 실 API·캐시 검증 완료.
- **팔당댐 방류량 카드 (v0.4.0, 2026-07-05)**: 현재 + 4시간 전(여의도 도달분) 방류량, 10분 3회
  개별 표시, 세로 구분선, 신선도 경고, **방류량 3000㎥/s 이상 운항금지 경고(두 열 독립 판정)**.
  실 API로 미리보기 검증 완료.
- 로컬 폴더 ↔ GitHub(OGISD/HanGangBUS) git 연결, push 배포 전환 (2026-06-22)
- 데이터 신선도 경고: 관측값 30분 이상 경과 시 카드 우상단 빨간 ⚠ (2026-06-22)
- 참고용 면책 문구 배너 상시 표시 (2026-06-22)
- AI Development OS 문서 체계 적용 (2026-07-04)

---

# 지금 진행 중이던 작업

- **한강 역류/순류 판정 = v1 완료, 실측 보정 대기(현재 최전선).** 판정 로직(추세+인천 조위 위상)·UI(종합 배지+화살표)는
  구현·검증 완료. 남은 건 **실제 역류 사례로 임계값(`FLOW.winMin`·`deadCm`) 보정** + 필요 시 720cm(신곡수중보) 게이팅.
  문구는 '경향형'으로 신중하게 유지 중.
- 그 외 카드(팔당댐·잠수교·여의도/인천 물때·조류세기·한강 수위 2열·통과높이)는 모두 완료·라이브. 대기 중인 회사 확정값 없음.

---

# 다음에 할 일

1. **역류 판정 임계값 실측 보정**: 실제 역류가 관측되는 날 `FLOW.winMin`(30분)·`deadCm`(2cm)이 맞는지 확인해 조정.
   과탐/미탐 시 인천 조위 720cm(신곡수중보) 게이팅 추가 검토(1019635 아라한강갑문으로 신곡보 수위 조회 가능).
2. 교량 추가 시: `DEFAULT_BRIDGES`에 항목 추가 + **CLAUDE.md 확정값 표·index.html Q&A·buildCards 삽입 규칙** 함께 갱신.
3. ~~확정 버전 git 태그 + GitHub 릴리즈~~ — **v0.6.0 태그 완료(2026-07-27)**, 인수인계 기준점. 다음 릴리즈부터 이어서.

※ 취소·확정(진행 안 함): 팔당댐 주의선(회사값 없음, `DAM.warnFlow=null`) / 교량 기준값 실측 반영 / 센서 이상값 방어 /
   팔당댐 3000 초과 핸드폰 알람(2026-07-15 사용자가 없던 일로 함 — 앱 무서버 구조상 진짜 푸시는 국내 IP 상시 서버 필요).

---

# 알아두어야 할 특이사항

- **API 키 유효기간(data.go.kr 2년)**: 물때 `2028-07-05`, 출몰시각 `2028-07-06` 만료.
  앱이 만료 60일 전부터 상단 배너로 경고(`API_EXPIRY`/`checkKeyExpiry`, index.html). **연장하면 그
  배열의 date만 새 만료일로 바꾸면 됨.** 사용자에게 개인 캘린더 알림도 백업으로 권함.
- **HRFCO API는 해외 IP 차단** — Cloudflare Worker·Vercel 등 무료 serverless 프록시 전부 불가
  (12초 타임아웃 후 522). 그래서 방문자 브라우저 직접 호출 구조이고, **API 키는 소스에 노출
  (의도된 결정)**. 어설픈 키 은닉 시도 금지 → [RULES.md](RULES.md) 금지 사항 참고.
- 해외에서 접속하면 자동 모드가 안 됨 → 수동 입력 폴백이 그 대비책.
- **물때 API(data.go.kr 1192136)는 `date` 파라미터를 무시하고 항상 서버 오늘(KST)만 반환.**
  필수 파라미터는 `obsCode`뿐. 응답은 XML(DOMParser 파싱). CORS 허용(*)이라 브라우저 직접 호출 OK.
  인천 `DT_0001`, 경인항 `DT_0058`(미사용). 키도 소스 노출(무료·읽기전용, HRFCO와 동일 판단).
- **출몰시각 API(B090041)는 물때와 같은 data.go.kr 계정 키 사용.** 2026-07-06 활성화 확인·연동 완료
  (그 전엔 403이었음 — 신규 활용신청 활성화 지연이었던 것). `location=인천`, 응답 XML.
- `hrfco-proxy-worker.js`는 폐기된 워커 잔재. 사용 안 함, 삭제해도 무방.
- 행주대교는 서해 조석 구간 → 썰물 때 수위 음수 정상. 급변 감지류 로직 넣을 때 오탐 주의.
- **사용자는 코딩 초보** — 멘토 모드(한 번에 한 단계, 완료 확인 후 진행) 필수. RULES.md 참고.
- 교량별 확정값·공식·검증 근거의 원본(source of truth)은 [CLAUDE.md](CLAUDE.md).

---

# 참고

- PRD: [PRD.md](PRD.md)
- 작업 목록: [TASKS.md](TASKS.md)
- 프로젝트 규칙: [RULES.md](RULES.md)
- 변경 이력: [CHANGELOG.md](CHANGELOG.md)
- 기술 맥락(공식·확정값·아키텍처 결정): [CLAUDE.md](CLAUDE.md)
