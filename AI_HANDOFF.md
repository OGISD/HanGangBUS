# AI_HANDOFF.md — 세션 인수인계

새 세션은 이 문서부터 읽고, 상세 기술 사양은 [CLAUDE.md](CLAUDE.md), 작업 현황은 [TASKS.md](TASKS.md)를 확인합니다.
과거 변경 이력은 [CHANGELOG.md](CHANGELOG.md)에 보존합니다.

## 프로젝트

- 앱 이름: **한강버스 운항 도우미**
- 목적: 한강버스 운항에 필요한 교량 통과높이·방류량·물때·수위·역류 경향을 모바일 한 화면에 표시
- 공개 주소: https://ogisd.github.io/HanGangBUS/
- 저장소: https://github.com/OGISD/HanGangBUS
- 배포: `main` 브랜치 push → GitHub Pages 자동 반영
- 최신 태그: `v0.7.0`(2026-08-03), 이후 변경은 `CHANGELOG.md`의 Unreleased에 기록
- Codex가 실질적으로 참여한 커밋은 `Co-authored-by: Codex <codex@openai.com>`으로 공식 GitHub 계정에 연결

## 현재 구현 상태

- 잠수교·행주대교 통과높이 및 운항 판정
- 팔당댐 현재·4시간 전 방류량, 3000㎥/s 이상 운항금지 표시
- 인천·여의도 물때, 조류세기
- 행주대교·한강대교 수위 최근 30분/과거 4시간
- 역류·순류 판정 v2(최소제곱 추세 + 두 다리 선행/지연 교차상관 + 인천 조위 위상)
- PWA 홈 화면 바로가기, 데이터 신선도 경고, 면책 문구, 관리자 진단줄
- 도움말 Q&A 아래 접이식 `운항 참고사항`: 썰물 때 행주대교 부근 정조와 마곡의 일시적 상류 방향 흐름 안내
- 앱 로직은 `index.html`, 큰 정적 물때 자료만 `data/*.json`으로 분리
- 라이브 제목 옆 작은 제작자 표기: `오동근, Claude·GPT와 함께`; 상세 역할은 `CONTRIBUTORS.md`
- `v0.7.0` 이후 승인된 안정화: 운항금지선 `<=`, API 최대 대기시간, 숨김 탭 호출 중지, 물때 30분 재시도,
  조류예측 일부 응답 캐시 방지, 유지보수 정리, 자동검사

## 2026-08-03 v0.7.0 주요 변경: 물때 API 장애 백업

- data.go.kr 물때 API가 브라우저 Origin 요청에 403/CORS를 반환한 사례를 확인함.
- 공공 API를 계속 우선 호출하고, 실패하면 같은 GitHub Pages의 연간 조석표 JSON으로 자동 전환함.
- `data/tide-incheon-2026.json`: 2026년 8~12월 153일
- `data/tide-incheon-2027.json`: 2027년 전체 365일
- 카드 우측에 `MM/DD · 온라인` 또는 `MM/DD · 연간표` 표시. 둘 다 예보이므로 `실시간`이라고 부르지 않음.
- 연간표는 API 캐시에 넣지 않음. 장애 뒤 자동 재시도는 30분 간격이며 수동 새로고침은 즉시 재시도함.
- 오늘 첫 물때의 증감 기준인 어제 마지막 조위가 `hb_tidelast`에 없으면 연간표에서 자동 보충함. 전날 앱 미사용이나
  온라인·연간표 전환에도 첫 행 증감이 유지되며, 자료 시작 전날이 없는 `2026-08-01`은 예외.
- 원본 월별 텍스트는 저장소에 포함하지 않음. 재생성 도구는 `tools/convert-tide.ps1`.
- 온라인 페이지에서 연간표 전환, 인천/여의도 값, 역류 위상 반영을 검증함.

## 안전 관련 확정값

| 항목 | 잠수교 | 행주대교 |
|---|---:|---:|
| 관측소 | 1018680 | 1019630 |
| base | 11.76 | 13.40 |
| offset | +0.02 | 0 |
| 운항금지선 | 7.3 | 10.9 |
| 경고선 | 7.8 | 10.9(별도 경고 단계 없음) |
| 저수위경고선 | 9.1 | 없음 |

- 교량 저장 키: `hb_bridges7`
- 팔당댐: 코드 `1017310`, 운항금지 `3000㎥/s`, 주의선 없음(`warnFlow:null`)
- 데이터 신선도: 30분
- 역류 추세: `FLOW_TUNING={winMin:30, deadRate:4}`. 2026-08-05 약 44시간 자료 비교에서 40분보다 전환 포착이 나아 30분으로 조정.
- 당분간 민감도를 자주 검토할 수 있으므로 `index.html`의 `FLOW_TUNING` 두 값만 바꿉니다. 도움말·관리자 진단은 자동 반영됩니다.
- 다리 라벨 옆 `↑/↓/↔ Ncm/1h`는 `hourlyChange()`가 최신 관측과 정확히 60분 전 관측의 실제 차이를 표시합니다.
  역류 판정용 30분 최소제곱 기울기와는 별개이며, 정확한 60분 전 값이 없으면 보간하지 않고 숨깁니다.
- 행주·한강대교 중 하나라도 최신 수위가 30분 이상 오래됐거나 없으면 배지에 판정 중단을 표시하고 추세 숫자를 숨김
- 시간차 판정: `minCorr:0.5`, `minAmpCm:5`, 최대 선행/지연 60분
- 통과높이가 운항금지선과 같아도 운항 금지(`pass <= noGo`)

## API·네트워크 주의사항

- HRFCO는 해외 IP를 차단하므로 방문자 브라우저가 직접 호출함. 해외 무료 Worker/서버리스 프록시 재도입 금지.
- HRFCO 키는 공개 저장소의 `index.html`에 있으며, 무료·읽기 전용 성격을 감수한 의도된 결정임.
- 해외 접속 등 HRFCO 자동 호출 실패 시 교량 수위는 수동 입력으로 사용 가능.
- 물때 API는 서버 요청에는 정상 XML을 반환했지만 2026-08-03 브라우저 Origin 요청이 차단된 사례가 있었음.
- 2026-08-04에는 브라우저 CORS가 정상화됐지만 형식 미지정 요청이 명세상 기본 XML 대신 JSON으로 반환됨.
  요청에 `type=xml`을 명시하고 `parseTideApi()`가 XML·JSON을 모두 해석하도록 보강해 서버 형식 변동에도 온라인 자료를 사용함.
- 물때 API 키 만료: 2028-07-05. 조류세기 KHOA 키 만료: 2027-07-06.
- 만료 60일 전부터 `API_EXPIRY`/`checkKeyExpiry()`가 상단 경고를 표시함.
- HRFCO/물때/조류예측 최대 대기시간은 각각 8초/3초/6초. 고정 대기가 아니라 응답 정지 시 상한임.
- 새로고침 버튼은 핵심 HRFCO 호출 완료만 기다림. 물때·조류예측은 카드별로 따로 갱신함.
- 숨긴 페이지에서는 자동 호출을 쉬고 다시 보이면 한 번 갱신함.
- 일출·일몰·월출·월몰과 해외 프록시 관련 죽은 코드는 제거됨.

## 현재 진행 중·다음 할 일

1. API 오류 때 이전 화면값을 유지·표시하는 방식은 심층 논의가 필요해 보류. 승인 전 변경 금지.
2. 2028년 조석표가 발간되면 기존 변환 도구로 `data/tide-incheon-2028.json` 추가.
3. 교량 추가 시 `DEFAULT_BRIDGES`, Q&A, 확정값 표, 카드 삽입 규칙을 함께 갱신.

실제 역류 사례를 통한 임계값 보정은 전문 검증이 가능할 때만 검토하는 선택적 장기 과제이며 필수가 아님.

채택하지 않은 항목: 신곡수중보 720cm 게이팅, 조위 코사인 보간, 팔당댐 정량 보정, 센서 이상값 방어,
팔당댐 초과 휴대전화 푸시 알람. 재검토하려면 사용자와 근거부터 확인합니다.

## 주요 파일

- `index.html`: 웹앱 UI·로직
- `data/tide-incheon-2026.json`, `data/tide-incheon-2027.json`: 물때 장애 백업
- `tools/convert-tide.ps1`: 월별 조석표 ZIP 검증·변환
- `tools/check-project.ps1`: 코드·확정값·조석표 무결성 검사
- `CONTRIBUTORS.md`: 기획·제작, AI 협업, 현장 자문 참여자
- `.github/workflows/check.yml`: push·PR 자동검사
- `AGENTS.md`: Codex 작업 규칙과 핵심값
- `CLAUDE.md`: 상세 기술 사양
- `TASKS.md`: 현재 작업 목록
- `RULES.md`: 프로젝트 작업 규칙
- `PRD.md`: 요구사항
- `CHANGELOG.md`: 변경 이력
