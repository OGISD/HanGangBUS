# Changelog

이 프로젝트의 주요 변경사항을 기록합니다. 형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.0.0/)를 따르며, 버전은 [Semantic Versioning](https://semver.org/lang/ko/)을 따릅니다.

---

## [Unreleased]

### Changed (2026-08-06, 실제 1시간 수위 변화 표시)
- 한강 수위 카드의 각 다리 옆 숫자를 최근 30분 최소제곱 기울기의 시간당 환산값에서, **최신 관측 수위와 정확히
  1시간 전 관측 수위의 실제 차이**로 변경하고 단위를 `cm/1h`로 명확히 표시.
- 정확히 1시간 전 관측이 없으면 보간·배수 환산하지 않고 숫자를 숨김. 역류 판정 자체는 기존 30분 추세와
  `±4cm/h` 임계를 그대로 사용하므로 판정 민감도에는 영향 없음. 카드 제목도 `한강 수위 · 판정 30분`으로 바꿔
  30분 판정과 1시간 변화 표시를 구분.

### Changed (2026-08-05, 역류 추세 민감도 조정 준비)
- 2026-08-04~05 약 44시간의 행주대교·한강대교 10분 수위를 비교한 결과, 40분 평균에 전환 직후 변화가 묻히는 사례가
  반복되어 최소제곱 추세 계산 구간을 **30분으로 단축**. 정체 임계 `±4cm/h`와 두 관측소의 30분 신선도 차단은 유지.
- 민감도를 당분간 자주 조정할 수 있도록 핵심값을 `FLOW_TUNING` 한곳에 분리하고, 도움말의 계산 구간·임계값이
  설정을 자동 반영하게 변경. 관리자 진단줄도 기존처럼 실제 적용값을 표시해 비교와 롤백이 쉽도록 유지.

### Fixed (2026-08-04, 물때 API 응답 형식 변동)
- 공공데이터포털 API가 형식 미지정 요청에 명세상 기본 XML 대신 JSON을 반환하면서 정상 자료를 `값 없음`으로 처리하던 원인을 수정.
- 요청에 `type=xml`을 명시하고 XML·JSON 응답을 모두 해석해, 서버 기본 형식이 다시 바뀌어도 온라인 물때를 우선 사용하도록 보강.

### Changed (2026-08-04, 역류 추세 민감도 복원)
- 실사용과 하루치 276개 판정을 비교한 결과, 60분 추세가 흐름 전환 시 상승·하강을 정체로 늦게 표시하는 사례가 반복되어
  최소제곱 추세 계산 구간을 **40분으로 복원**. 정체 임계 `±4cm/h`와 두 관측소의 30분 신선도 차단은 그대로 유지.

### Changed (2026-08-03, GitHub 기여자 연결)
- Codex가 실제 변경에 참여한 커밋에 `Co-authored-by: Codex <codex@openai.com>`을 사용하도록 규칙화해
  GitHub 저장소 Contributors 목록에서도 공식 `codex` 계정으로 표시되도록 정리.

### Added (2026-08-03, 참여자 표기)
- `CONTRIBUTORS.md`를 추가해 오동근의 기획·제작, Claude·GPT의 AI 협업, 현장 자문·실측 참여자를 역할별로 정리.
- 라이브 제목 옆 작은 글씨의 어색한 `오동근 (with 클로드)`를 `오동근, Claude·GPT와 함께`로 바꾸고 도움말 표기도 통일.

### Changed (2026-08-03, 역류 추세·신선도)
- 역류 추세 계산 구간을 최근 40분에서 **60분**으로 늘려 짧은 변동의 영향을 완화.
- 행주대교·한강대교 중 하나라도 최신 수위가 30분 이상 오래됐거나 없으면 역류 판정과 추세 숫자를 중단하고,
  같은 배지 공간에 안내한 뒤 새 관측값이 들어오면 자동 재개.
- 추세 구간은 이후 실사용 비교에서 전환 감지가 둔해지는 점이 확인되어 2026-08-04에 40분으로 복원함.

### Changed (2026-08-03, 조류세기 표기 롤백)
- 실사용 의견에 따라 `조류세기(상대)` 표기를 기존의 간결한 `조류세기`로 되돌리고, 도움말은 예측 참고값이라는 점만
  짧게 안내하도록 정리. 계산식과 일부 응답 캐시 방지 로직은 그대로 유지.

### Changed (2026-08-03, 안정화·호출 최적화)
- 교량 통과높이가 운항금지선과 같은 경우도 **운항 금지**가 되도록 경계를 `<`에서 `<=`로 변경.
- 경계값의 차이가 부동소수점 오차로 `-0.00m`처럼 표시되지 않도록 숫자 표시를 정규화.
- API 최대 대기시간을 HRFCO 8초·물때 3초·조류예측 6초로 설정. 평소 응답은 즉시 표시하고 멈춘 요청만 상한에서 종료.
- 새로고침 버튼이 고정 0.8초가 아니라 핵심 HRFCO 호출 완료 뒤 결과를 표시하도록 변경. 물때·조류예측은 버튼 완료를 지연시키지 않음.
- 숨긴 페이지에서는 자동 호출을 중지하고 다시 보이면 한 번 갱신. 물때 API 장애 자동 재시도는 10분마다에서 30분마다로 줄이고,
  사용자가 누른 새로고침은 대기시간과 무관하게 즉시 재시도.
- 여의도 만조를 `인천 만조 약 +4시간`의 **추정값**으로 명시. KHOA 8개 시각 중 일부만 응답하면 화면에는
  참고 표시하되 하루 캐시에 저장하지 않아 다음 갱신에서 다시 조회.

### Fixed (2026-08-03, 설정·안내)
- 설정에서 저수위경고선을 빈칸으로 저장해도 이전 값이 남던 문제를 수정해 실제로 `없음` 처리.
- 데이터 키 만료 배너가 물때는 공공데이터포털, 조류예측은 KHOA 바다누리의 발급처를 구분해 안내하도록 수정.
- 새로고침 상태에 접근성 속성을 추가하고 키보드 초점 표시를 보강.

### Removed (2026-08-03, 유지보수 정리)
- 작동하지 않는 allorigins/Cloudflare 해외 프록시 설정과 프록시 관련 관리자 입력란, 폐기된 `hrfco-proxy-worker.js` 제거.
- 호출되지 않던 출몰시각(`fetchRiseSet`) 코드와 외부 Pretendard 글꼴 CDN 의존성 제거.

### Added (2026-08-03, 자동검사)
- `tools/check-project.ps1`: JavaScript 문법, 통과높이 공식·경계·확정값, 장애 백업·캐시 규칙,
  2026~2027 연간 조석표 날짜 연속성·형식·고저조 교대를 한 번에 검사.
- `.github/workflows/check.yml`: push·PR·수동 실행 때 같은 검사를 자동 수행.

## [0.7.0] - 2026-08-03

### Added (2026-08-03, 운항 참고사항 카드)
- 도움말 Q&A 아래에 같은 방식으로 여닫는 `운항 참고사항` 카드 추가.
- 첫 항목으로 썰물 때 행주대교 부근에서 약 1시간 수위 변화가 약해지는 정조 현상과, 이때 마곡에서 일시적으로
  밀물처럼 보이는 상류 방향 흐름이 나타날 수 있음을 안내. 한강대교 값만 보지 말고 행주대교 수위와 현장 흐름을
  함께 확인하도록 명시.

### Fixed (2026-08-03, 첫 물때 증감 유지)
- 브라우저에 어제 마지막 조위가 없을 때 내장 연간표에서 해당 날짜의 마지막 물때를 찾아 오늘 첫 행의 증감 기준으로 사용.
- 전날 앱을 열지 않았거나 온라인 API와 연간표가 전환되어도 첫 물때 변화량이 누락되지 않도록 개선.
  연간표 시작 전날 자료가 없는 `2026-08-01`은 기존처럼 증감 없이 표시.

### Changed (2026-08-03, 인수인계 문서 정합성 정리)
- 실제 코드 기준으로 README와 인수인계 문서의 오래된 설명을 정리: 역류 판정 v2 임계값(40분·4cm/h),
  물때 API CORS 장애·연간표 백업, 교량 저장키 `hb_bridges7`, 미사용 출몰시각, 정적 JSON 파일 구조 반영.
- `AI_HANDOFF.md`를 현재 상태 중심으로 축약하고, 과거 상세 이력은 `CHANGELOG.md`를 기준으로 보존.

### Added (2026-08-03, 물때 API 장애 백업)
- 공공데이터포털 물때 API를 우선 사용하되, CORS/통신 장애 시 **국립해양조사원 연간 조석표 JSON으로 자동 전환**.
- 인천 `DT_0001` 조석표를 `data/tide-incheon-2026.json`(8~12월)·`data/tide-incheon-2027.json`(전체)으로 분리해
  같은 GitHub Pages에서 읽음. 원본 월별 텍스트는 저장소에 포함하지 않음.
- 인천·여의도 카드의 기존 우측 날짜 자리에 `MM/DD · 온라인` 또는 `MM/DD · 연간표`를 표시해 세로 공간 증가 없이 출처 구분.
- API 응답 날짜·값 검증, 연간표 스키마·관측소·단위 검증, API 복구 시 다음 새로고침에서 자동 복귀.
- 월별 텍스트 ZIP을 연도별 JSON으로 검증·변환하는 `tools/convert-tide.ps1` 추가.

### Added (2026-07-27, Q&A 보강)
- 도움말 Q&A에 **최근 기능 설명 3개 추가**(17→20항목). 기존 항목은 코드값과 전수 대조해 **오류 없음** 확인.
  - **역류/순류 배지**(🔺역류 경향/🔻순류/⏸정체·전환점) 각각의 뜻·판정 근거·'경향'으로만 쓰는 이유.
  - **추세 화살표(cm/h)와 '행주 선행 N분'** — 1시간당 오르내리는 속도(±4cm/h 안쪽은 ↔), 먼저 움직인 쪽으로
    조석/방류 원인을 '추정'한다는 설명, 잔잔하면 표시 안 됨.
  - **홈 화면 바로가기** 추가 방법(안드로이드/아이폰 차이, ✕ 눌러도 일주일 뒤 재표시).
  - 카드 목록의 '한강 수위' 줄에 '+ 역류/순류 판정 표시' 추가.
  ※ 관리자 진단줄·이스터에그는 의도적으로 Q&A에 넣지 않음(숨은 기능).

### Changed (2026-07-27, 문구 개선 + 관리자 진단줄)
- **"서명" 용어 제거**: 역류 근거줄의 "조석 서명"/"상류 서명"(signature 직역이라 이질적)을
  **"조석 영향 추정"/"방류 영향 추정"** 으로 교체. 원인은 두 다리 시간차로 미루어 짐작하는 것이므로
  **"추정"을 명시해 확정이 아님을 매번 알림**. 예: "행주 선행 40분(조석 영향 추정)".
- **관리자 진단줄 추가**(`#flowDiag`): 한강 수위 카드 맨 아래에 판정에 쓴 원시 수치(각 다리 cm/h·표본수,
  임계값, 선행 방향·분, 상관계수, 진폭, 위상)를 한 줄로 표시. **`?admin`에서만 보이고 일반 방문자에겐 숨김**
  (기존 `applyAdmin()` 토글 재사용). 실제 역류 목격 시 캡처해 임계값 실측 보정 근거로 쓰기 위함.

### Changed (2026-07-27, 역류 판정 v2 — 시간차 교차검증 + 추세 견고화)
- **두 다리 시간차(선행/지연) 교차검증 추가**(`leadLag`): 행주·한강대교의 움직임(1차 차분)을 10분 격자로 교차상관해
  **누가 먼저 움직였는지** 판정. **행주 선행 = 조석(역류) 서명**, **한강대교 선행 = 상류(팔당댐 방류) 서명**.
  근거줄에 "행주 선행 N분(조석 서명)"으로 표시하고, 역류 판정인데 한강대교가 선행이면 "상류(방류) 영향 가능"을 덧붙여
  단정을 낮춤 → **방류 급증을 역류로 오해하던 약점을 원리적으로 보완**. 진폭 5cm 미만·상관 0.5 미만이면 판정 보류.
  부호 규칙은 합성 데이터로 검증(행주 30분 선행 → +30분, 상관 1.0). 실측에서 행주 선행 40분·상관 0.85 관측.
- **추세 계산 견고화**: 2점 차분 → **최근 40분 최소제곱 기울기(cm/h)**. 끝값 하나가 튀어도 흔들리지 않고,
  두 다리를 같은 잣대(cm/h)로 비교 가능. 시각을 실제 타임스탬프로 계산해 결측이 섞여도 안전. 화살표도 cm/h로 표시.
- 둘 다 **새 API·추가 호출 0**(이미 받는 4시간치 수위 재활용).

## [0.6.0] - 2026-07-27

> 카드 확장·역류 판정·최적화 통합 릴리즈. v0.5.0(물때 카드) 이후 누적분을 확정하고 인수인계 기준점으로 태그.
> 주요: 조류세기 KHOA 실측 전환 · 한강 수위(행주+한강대교 2열) · **한강 역류/순류 판정 v1** · 홈 화면 바로가기(PWA) ·
> 이스터에그 · 행주 운항금지선 10.9m · API 최적화(물때 캐시·행주 중복 제거) · Q&A/문서 정합성 정정.

### Changed (2026-07-15, API 최적화)
- **물때(고저조) 하루 1번만 조회·캐시**: `fetchTide`가 매 새로고침(10분)마다 같은 하루치 예보를 재요청하던 낭비를
  제거. `hb_tide`(ymd 키)에 캐시 → 조류세기와 동일 패턴. 캐시 히트 시 네트워크 없이 화면·역류 위상만 갱신.
  탭 종일 기준 물때 요청 ~144회/일 → 1회/일. (HRFCO는 새로고침당 4회로 이미 최소 — 추가 낭비 없음 확인.)

### Added (2026-07-15, 한강 역류/순류 판정 v1)
- 한강 수위 카드 상단에 **역류/순류 종합 배지**(🔺역류 경향 · 바다→한강 / 🔻순류 · 한강→바다 / ⏸정체·전환점)
  + 각 다리 옆 **추세 화살표(↑/↓/↔ Δcm)**. **판정 = 두 다리 수위 추세 + 인천 조위 밀/썰물 위상 교차검증**.
  밀물이면서 행주 수위 상승이면 역류 경향(한강대교도 상승이면 '여의도까지'), 썰물이면서 하강이면 순류,
  상충·정체면 '정체·전환점'(경향형 표기). **새 API·추가 호출 없음**(이미 받는 수위·물때 데이터 재활용).
  신규 `wlTrend()`/`tidePhaseNow()`/`renderFlow()`, 전역 `flow`, 상수 `FLOW`(winMin 30·deadCm 2, 실측 후 보정).

### Changed (2026-07-15, 이어서)
- **행주대교 운항금지선 11.2 m → 10.9 m**(warn도 =noGo 유지 → 여전히 여유/금지 2단계). 상태줄 "운항금지선(10.90m)까지"는
  자동 반영. Q&A '왜 행주엔 경고가 없나' 항목의 금지값도 10.9 m로 갱신. 저장 키 hb_bridges6→hb_bridges7(새 값 강제 적용).

### Changed (2026-07-15)
- **이스터에그(크레딧 마퀴) 개선**: 글자 크기 반응형(`clamp(11px,3.5vw,16px)` — 큰 화면 16px 유지, 작은 폰에서만
  완화해 '부담' 해소), 앞머리 ⚓→배 ⛴(위아래로 통통 뜨는 bob 애니), 마퀴 띠에 은은한 물빛 배경.
  등장 시점 30초→**1분**. 이름·역할 문구는 그대로.

### Fixed (2026-07-15)
- **Q&A 허위 기재 수정**: 직전 커밋(4ca693a)에서 인천 카드 설명에 넣은 **"일출·일몰·월출·월몰"은 실제로
  없는 기능**이라 삭제. (2026-07-07 조류세기로 교체되며 제거됨 — `#sunMoon` HTML 없음, `fetchRiseSet()`은
  호출되지 않는 죽은 코드.) 원인은 CLAUDE.md의 모순된 기록("인천 카드에 통합·fetchAll에서 호출") → **CLAUDE.md도
  '❌ 없는 기능'으로 정정**하고 index.html 죽은 코드 주석도 보강.
- Q&A 정확도: 자동/수동 항목에 **한강 수위도 자동 모드 전용**임을 명시(팔당댐만 적혀 있었음).
  팔당댐 Q에 실제 있는 '과거 4시간 기록' 펼치기 언급 추가.

### Changed (2026-07-13, 이어서)
- **한강 수위 카드 안내 정리 + Q&A 개정**: 카드 `#wlNote`에서 '한강대교=마포대교 대체' 설명을 빼고
  ("※ 역류 판정을 위한 참고 정보이며 개선 중"만 남김), 그 내용을 도움말 Q&A 새 항목('한강 수위 카드는 뭔가요?')으로 이전.
  또 **조류세기 설명을 실제 방식(KHOA 수치조류도 예측 유속의 오늘 최고값)으로 정정**(옛 '조차 추정·실측 아님'은 오류였음),
  카드 목록에 '한강 수위' 카드 추가·인천 카드에 일출/일몰 언급, 인트로에 '한강 수위' 추가.
- **홈 화면 바로가기 버튼 ✕: 영구 숨김 → 7일 스누즈**. 실수로 눌러도 일주일 뒤 자동으로 다시 표시(영구
  삭제 방지). `hb_a2hs`에 만료 timestamp 저장, 툴팁 "일주일간 숨기기". 옛 `'0'`값은 자동으로 다시 보임.
- **행주대교 HRFCO 중복 호출 제거(5회→4회/새로고침)**: 행주(1019630)를 통과높이용(최신1건)·수위카드용
  (4시간 기간) 두 번 부르던 것을, 4시간 조회 1건의 최신값으로 통과높이 카드까지 함께 렌더해 통합.
  `fetchWL`에 선택적 `bridge` 인자 추가, `fetchAll` 교량 루프는 행주 건너뜀. UI·표시 결과는 동일.
  (하루 한도 1만 회 기준 총 새로고침 2,000→2,500회로 여유 증가.)
- **행주대교·한강대교 수위 카드를 한 카드(`#wlCard`)로 합침** — 팔당댐 방류 카드처럼 좌우 2열(왼쪽 행주대교,
  오른쪽 한강대교, 세로 구분선). "과거 4시간 기록"도 2열. 신선도는 카드 공용 `#wlT` 하나(두 관측소 같은 주기).
  공간 절약 + 두 수위를 한눈에 비교. 예전 `#backflowCard`/`#hangangCard` 폐기.

### Added (2026-07-13)
- **홈 화면 바로가기 버튼**(`#a2hs`): 면책 문구 아래에 "📲 홈 화면에 바로가기 추가" 슬림 바.
  안드로이드/크롬은 누르면 네이티브 설치창(`beforeinstallprompt`), 아이폰(사파리)은 자동 추가 불가라
  "공유 → 홈 화면에 추가" 안내가 펼쳐짐, 그 외 브라우저는 메뉴 안내. 이미 설치(standalone)면 자동 숨김,
  ✕로 닫으면 그 기기에서 다시 안 뜸(`hb_a2hs`).
- **한강대교 수위 카드**(`#hangangCard`): 행주대교 수위 카드 밑에 추가. 최근 30분(10분 3개) 요약 +
  과거 4시간 펼치기(행주 수위 카드와 동일 서식). **마포대교 지점 관측소가 HRFCO에 없어**(서울 본류는
  잠수교/한강대교/행주대교만) 가장 가까운 한강대교(1018683, 마포 상류 ≈3km, 수위 사실상 동일)로 대체 —
  카드 라벨·안내에 명시.
- 행주/한강대교 수위 로직을 **공용 함수로 일반화**: `wlUrl()/fetchWL()/renderWL()` 하나로 두 관측소 처리
  (기존 `BACKFLOW/backflowUrl/fetchBackflow/renderBackflow` 대체). `fetchAll`에서 코드만 바꿔 두 번 호출.

### Changed (2026-07-13)
- "과거 4시간 기록" 등 접이식 화살표(▼/▲) 크기 확대(다른 UI와 비례).
- 새로고침 버튼에 진행/완료 피드백 추가: 누르면 "⟳ 새로고침 중…"(파랑) → "✓ 새로고침됨"(초록) → 원상복귀.

### Added (2026-07-08)
- 팔당댐·행주대교 수위 카드에 **"과거 4시간 기록" 펼치기**(접이식+스크롤). 10분 단위 ~25개(4시간 연속) 표시.
  팔당댐은 이미 받던 데이터로 표시만 추가, 행주는 조회 구간 30분→4시간(`BACKFLOW.winMin` 240, 요약은 최신 3개 유지).
  공통 `histRows()/within4h()`, 스크롤 `.drows.scroll`(max-height 190px).

### Added (2026-07-08 한강 역류 감시)
- 행주대교 카드 밑에 **한강 역류 감시 카드**(`#backflowCard`): 행주대교 수위 최근 30분(10분 단위 3개,
  팔당댐 서식, 최신 강조 + 신선도). HRFCO 1019630 기간조회. buildCards가 행주대교 카드 바로 아래로 배치.
  ※ 역류 자동 판정(인천 조위 720cm=신곡수중보 초과)은 실측 검증 후 추가 예정.
  배경: 인천 조위>720cm면 밀물이 신곡수중보 넘어 상류 역류. 행주 절대수위 고정 임계값은 팔당댐 방류에
  따라 기준선이 변해 불안정 → 판정은 인천 720cm 기준으로 검증 후 도입 예정.

### Added/Changed (2026-07-07 안전 문구)
- 잠수교 **저수위 경고**: 통과높이가 9.1m(`lowWarn`)를 넘으면(수위 매우 낮음=저수심 위험) 주황 '저수위 경고'
  + "저수심 위험 · 저수위경고선(9.10m) 초과 X m". 설정에서 조정 가능. (행주는 lowWarn 없음)
- 행주대교 상태줄 접두어(`noGoPrefix`) "마스트 장착시 " → "마스트 장착시 운항금지선(11.20m)까지 X m".
- 교량 저장키 hb_bridges5→hb_bridges6(새 필드 강제 적용). Q&A에 저수위 경고 설명 추가.

### Changed (2026-07-06 직원 피드백 2차 · UI 다듬기)
- 헤더 한 줄로 정리: 제목 옆에 제작자(· 오동근 with 클로드), 오른쪽에 "다리 그림 숨기기" 토글 → 줄 낭비 제거.
- 조류세기 게이지 막대를 텍스트와 수직 중앙 정렬(막대 top:5px 보정, 높이 12px).
- 다리 그림 추가 축소(150→120px, 통과높이 폰트 38→30px) — 잠수교·행주대교 여백 절약.
- 하단 안내 문구 삭제(빈 `#footNote`로 자리만 보존).
- 팔당댐 3000㎥/s 초과 경고에서 "여의도" 제거 → 4시간 전 열은 그냥 "운항금지".
- 도움말 제목 "도움말 · 초간단 Q&A"→"도움말 · Q&A". Q&A 전면 개정: 각 카드 기능,
  색상 상태(여유/경고/금지) 의미와 다리별 차이, 용어(통과높이·gdt·오차보정·조류세기 등) 설명 추가.

### Changed (2026-07-06 직원 피드백)
- 앱 이름 "한강버스 운항 도우미"로 변경, 상단에 "만든이 오동근 (with 클로드)" 표기.
- 잠수교: 운항금지선 7.3m, 운항경고선 7.8m, 통과높이 오차보정 +2cm(설정에서 조정 가능).
- 행주대교: 운항금지선 11.2m, 운항경고 단계 제거(여유/운항금지 2단계).
- 중간 경고 단계 라벨을 "위험"→"운항 경고"(주황)로 명확화. warn≤noGo면 경고 단계 미표시.
- 상태줄 문구 "금지선까지 X m" → "운항금지선(7.30m)까지 X m"(데드라인 숫자 명시).
- 여의도 물때를 하루 4개→만조 2개만으로 되돌림.
- 다리 그림(잠수교·행주대교) 축소(200→150px) + 상단 "다리 그림 숨기기" 온/오프 토글(기기에 기억).
  그림을 숨겨도 통과높이 숫자는 축소 형태(`.cpass`)로 계속 표시.
- 도움말 Q&A를 위 변경에 맞게 갱신. 교량 저장 키 hb_bridges4→hb_bridges5(새 임계값 강제 적용).

### Changed (조류세기 — KHOA 실측 '오늘 하루 세기')
- 조류세기를 **KHOA 수치조류도 예측**의 **오늘 최고 유속** 기준 '하루 세기 %'로 표시(바다타임식, 하루 고정).
  오늘 59%(바다타임 일치). 방향 화살표 없이 깔끔한 %. 하루 여러 시각 스캔 후 최고값 → maxSpeed(125) 대비 %.
  **하루 1번 계산·localStorage 캐시(`hb_tidecur`)로 호출 절약.** (실시간 유속 방식은 정조 땐 ~0이라 혼란 → 폐기.)
  KHOA 바다누리 자체 키(data.go.kr와 별개, ~2027-07-06). CORS 허용, JSON. 조차 추정치도 폐기.
- API 만료 목록: 미사용 출몰 제거, 조류세기 키(2027-07-06, 최단) 추가.

### Fixed
- 출몰시각 정렬: ☀/☾ 아이콘 폭 차이로 윗줄·아랫줄이 어긋나던 것을 고정폭 grid로 교정.

### Added
- 일출·일몰·월출·월몰: 인천 물때 카드 하단에 통합(별도 카드 아님). 한국천문연구원 출몰시각 API
  (data.go.kr B090041, location=인천), XML→DOMParser, CORS 직접 호출. 물때와 같은 계정 키 사용.
- API 키 만료 사전 경고: 물때(2028-07-05)·출몰시각(2028-07-06) 키가 만료 60일 전부터 상단 배너로
  "미리 연장하세요" 알림(`API_EXPIRY`/`checkKeyExpiry`). 연장 시 배열의 date만 갱신.
- 물때 카드 화살표·증감 표시: 각 행에 만조 ▲/간조 ▼ + 직전 물때 대비 증감(cm, 내려가면 음수 예: -658).
  여의도를 만조 2개→하루 물때 4개 전부(+4시간)로 확대. 첫 행 증감은 어제 마지막 물때를 localStorage에 캐시해 계산.
- (대기) 팔당댐 주의선(2번째 절대값) — 회사 확인 후 `DAM.warnFlow`에 숫자만 넣으면 활성
- (대기) 일출·일몰·월출·월몰 — 한국천문연구원 출몰시각 API(RiseSetInfoService). **물때 카드 안에 통합**(별도
  카드 아님). 현재 키가 이 API에는 403(활성화 대기) — 승인·활성 후 구현.
- (연기) 센서 이상값 방어 1단계 — 기능 확장 뒤로 미룸

---

## [0.5.0] - 2026-07-06

### Added
- 인천 물때 카드: 국립해양조사원 조석예보(고저조) API로 인천(DT_0001)의 오늘 만조·간조 4개
  (시각·조위 cm) 표시. data.go.kr(1192136) 사용, XML 응답을 DOMParser로 파싱, CORS 허용이라 브라우저 직접 호출.
- 여의도 물때 카드: 인천 만조 시각 +4시간 = 여의도 만조 2개(자정 넘으면 "익일" 표기).
- 카드 배치: 팔당댐 → 잠수교 → 여의도물때 → 행주대교 → 인천물때 (buildCards가 교량 카드만
  재생성하고 물때 카드 사이에 삽입). 물때는 예보값이라 수동 모드에서도 표시.

---

## [0.4.0] - 2026-07-05

### Added
- 팔당댐 방류량 카드 (최상단): 현재 방류량 + 4시간 전 방류량(지금 여의도에 도달 중인 방류분).
  각 창은 10분 단위 3회 관측값을 **전부 개별 표시**(시각+값, 최신 행 강조), 두 열은 세로 구분선으로 분리,
  교량 카드와 동일한 데이터 신선도 경고. HRFCO 댐 API(팔당댐 1017310, tototf ㎥/s) 사용.
  수동 모드에서는 조회하지 않음(안내 문구 표시).
- 팔당댐 방류량 운항금지 경고: 각 열 최신 방류량이 3000㎥/s 이상이면 그 열에 빨간 경고
  ("여의도 운항금지" / "4시간 뒤 운항금지"). 주의선(2번째 절대값)은 회사 확인 후 추가 예정.

---

## [0.3.0] - 2026-07-04

### Added
- AI Development OS 문서 체계 적용: PRD.md, TASKS.md, RULES.md, AI_HANDOFF.md, CHANGELOG.md

---

## [0.2.0] - 2026-06-22

### Added
- 데이터 신선도 경고: 자동 모드에서 관측값 경과시간 표시("갱신 HH:MM · N분 전"),
  30분 이상이면 빨간색 + ⚠ (KST 기준 환산으로 기기 시간대 무관)
- 참고용 면책 문구 배너 상시 표시 ("실제 운항·통항 여부는 공식 정보로 직접 확인")
- `.gitignore`(zip·.claude 제외), `.gitattributes`(LF 정규화)

### Changed
- 배포 방식 전환: GitHub 웹에서 전체 복붙 → 로컬 git 저장소에서 `git push` (Pages 자동 재배포)
- CLAUDE.md에 "작업 완료 시 문서 갱신" 작업 규칙 추가

---

## [0.1.0] - 2026-06-21

### Added
- 최초 배포 (GitHub Pages: https://ogisd.github.io/HanGangBUS/)
- 통과높이 계산·표시: 잠수교·행주대교 카드, 수위 시각화(물 높이 애니메이션)
- 안전 판정: 운항 금지선(noGo)·위험 경고선(warn) 기준 색상/라벨
- 자동 모드(HRFCO 오픈API 직접 호출) / 수동 모드(수위 직접 입력) 전환
- 자동 새로고침(기본 10분), 설정 패널(교량별 값 편집, localStorage 저장)
- 설정 패널 잠금: 기본 숨김, `?admin` URL 또는 제목 5회 탭으로 해제
- 도움말 Q&A (통과높이·기준높이·gdt·임계값 설명)
